# Skipped: minimal has no VERILOG_FILES in config.mk (empty SDC test design).
filegroup(
    name = "design_config",
    srcs = glob(["*.mk", "*.sdc"], allow_empty = True),
    visibility = ["//visibility:public"],
)
