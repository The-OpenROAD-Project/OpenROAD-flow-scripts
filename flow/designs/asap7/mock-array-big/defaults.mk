# single source of truth for defaults.
# each number is a unit
# current unit is configured as 2.16 which is on the routing grid for M5

# Element size (width height)
export MOCK_ARRAY_ELEMENT_SIZE  ?= 4 4

# Element placement pitch (x y)
export MOCK_ARRAY_ELEMENT_PITCH ?= 6 6

# number of Element (rows cols)
export MOCK_ARRAY_TABLE         ?= 8 8

# Element'd data width
export MOCK_ARRAY_DATAWIDTH     ?= 8

